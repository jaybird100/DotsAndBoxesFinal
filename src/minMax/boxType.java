package minMax;

public enum boxType {
    // These are the box types that can make up a chain
    // chains are series of boxes with have only 2 lines in them which are connected
    sideways, longways, topleft,topright,bottomright,bottomleft;
}
